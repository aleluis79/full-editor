# openspec/config.yaml
# Auto-generated by sdd-init — 2026-07-14
schema: spec-driven

context: |
  Tech stack: React 19 + TypeScript 6 + Zustand 5 (frontend); FastAPI + SQLAlchemy 2 + Alembic + PostgreSQL + ReportLab (backend)
  Architecture: Full-stack rich text editor — no contentEditable, textarea + JS events, Canvas-based layout measurement, virtual-scrolled pagination
  Testing: Vitest with jsdom (frontend), pytest + httpx.TestClient + SQLite in-memory (backend)
  Style: Immutable document model, Command Pattern for operations, Zustand stores for state, layered architecture (State → Operations → Layout → Render)
  Conventions: Oxlint for linting, TypeScript strict mode, Python type hints via FastAPI/Pydantic

rules:
  proposal:
    - Include rollback plan for risky changes
    - Reference the specific subsytem affected (core/operations, layout, pagination, stores, API, PDF export)
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include data flow or sequence diagrams for complex interactions
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns and naming conventions
    tdd: true
    test_command: make test
  verify:
    test_command: make test
    build_command: make frontend-build
    coverage_threshold: 0
    frontend_test_command: make frontend-test
    backend_test_command: make backend-test
    backend_test_cov_command: make backend-test-cov
  archive:
    - Warn before merging destructive deltas
    - Sync both Engram and openspec artifacts
